PORT=8080

# 添加MySQL配置
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=147369  # 请根据实际MySQL密码修改
DB_NAME=hospital
DB_PORT=3306

# 允许的跨域来源，多个来源用逗号分隔
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# 是否允许携带凭证(cookies等)
ALLOW_CREDENTIALS=true

# JWT配置
JWT_SECRET=hospital_management_system_secret_key_2024
JWT_EXPIRES_IN=24h

# 短信服务配置
# 可选值: mock（模拟模式）、twilio（Twilio，支持个人手机号）
# 默认为twilio
SMS_SERVICE_PROVIDER=twilio

# Twilio短信配置（支持使用个人手机号）
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

