PORT=8080

# 添加MySQL配置
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=147369  # 请根据实际MySQL密码修改
DB_NAME=hospital
DB_PORT=3306

# 允许的跨域来源，多个来源用逗号分隔
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# 是否允许携带凭证(cookies等)
ALLOW_CREDENTIALS=true

# JWT配置
JWT_SECRET=hospital_management_system_secret_key_2024
JWT_EXPIRES_IN=24h

# 短信服务配置
SMS_SERVICE_PROVIDER=spug
# 是否启用真实短信发送（true/false）
SMS_REAL_SEND=true

# Spug推送助手配置
# 根据Spug推送助手文档，使用以下URL进行推送
# 格式: https://push.spug.cc/send/{token}
SPUG_PUSH_URL=https://push.spug.cc/send/XlxGd8J43Vj2bn0R

# Spug推送说明：
# 1. 验证码通过code参数传递
# 2. 目标手机号通过targets参数传递
# 3. 支持多个手机号，用逗号分隔
# 详细文档：https://push.spug.cc/guide/push/targets

